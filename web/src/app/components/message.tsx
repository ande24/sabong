'use client';

export default function Message({ message }: { message: string }) {
  return (
    <div className="w-full whitespace-nowrap text-white text-5xl ">
      <div className="animate-message inline-block">
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
        <span className="mx-2">{message}</span>
      </div>
    </div>
  );
}